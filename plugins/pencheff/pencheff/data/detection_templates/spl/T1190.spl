index=web sourcetype=access_combined
| eval suspicious=if(match(uri_path, "(?i)\.(env|git|svn|bak|swp)|/admin|/actuator|/debug"), 1, 0)
| where suspicious=1
| stats count by src_ip, uri_path, status
| where count > 5
| sort -count
