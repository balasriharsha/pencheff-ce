import { privateRouteMetadata } from "@/lib/seo";

export const metadata = privateRouteMetadata;

export default function AsmLayout({ children }: { children: React.ReactNode }) {
  return children;
}
